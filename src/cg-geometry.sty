\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{cg-geometry}[2026/08/27 Geometry]
\RequirePackage{geometry}

\geometry{
  paperwidth=150mm, paperheight=210mm, margin=10mm,
  top=11mm, bottom=15mm, headheight=15pt, headsep=6mm, twoside
}
\setlength{\columnsep}{4mm}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\emergencystretch}{15pt}
\tolerance=2000
\hbadness=1999

% Centralized Layout Dimensions
\newlength{\CGSummaryBoxSize}
\setlength{\CGSummaryBoxSize}{0.8cm} % Size of the grade summary colored squares
\newlength{\CGMarginWidth}
\setlength{\CGMarginWidth}{15mm} % Absolute left margin offset for full-bleed elements
\newlength{\CGRouteBadgeSize}
\setlength{\CGRouteBadgeSize}{6mm} % Dimensions of the route number color badge
\newlength{\CGRouteNumberWidth}
\setlength{\CGRouteNumberWidth}{8mm} % Minipage width for the route number column
\newlength{\CGZoneBannerHeight}
\setlength{\CGZoneBannerHeight}{62.5mm} % Clip height for the zone cover image banner
\newlength{\CGVerticalSpacingDefault}
\setlength{\CGVerticalSpacingDefault}{4mm} % Standard vertical spacing after blocks
\newlength{\CGSectorHeaderSpaceBefore}
\setlength{\CGSectorHeaderSpaceBefore}{2em} % Spacing before a sector header
\newlength{\CGSectorHeaderSpaceAfter}
\setlength{\CGSectorHeaderSpaceAfter}{1em} % Spacing after a sector header

\endinput
