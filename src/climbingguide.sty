\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{climbingguide}[2026/09/02 Climbing Guide Core]

\RequirePackage{xcolor}
\RequirePackage{graphicx}
\RequirePackage[export]{adjustbox}
\RequirePackage{multicol}
\RequirePackage{longtable}
\RequirePackage{array}
\RequirePackage{mwe}
\RequirePackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=brand_color!60, urlcolor=brand_color!60, citecolor=brand_color!60}

% --- Consolidated Domain Modules ---
\RequirePackage{cg-theme}       % Design tokens, fonts, geometry
\RequirePackage{cg-header}      % Headers and Footers
\RequirePackage{cg-components}  % UI Boxes, Macros, and Lists
\RequirePackage{cg-frontmatter} % ToC and covers
\RequirePackage{cg-engine}      % Lua-LaTeX Bridge and State (expl3 isolated)

% --- Global LuaTeX Input Buffer Interception ---
\directlua{
  luatexbase.add_to_callback('process_input_buffer', function(line)
    return line:gsub(string.char(194, 160), ' ')
  end, 'sanitize_nbsp')
}

% --- Global Tolerances ---
\hfuzz=500pt \vfuzz=500pt
\hbadness=10000 \vbadness=10000
\tolerance=2000 \emergencystretch=20pt

\endinput
