\section{System Architecture}

The engine enforces an absolute decoupling between the presentation layer (\LaTeX) and the data/logic layer (Lua). Production source files remain purely declarative databases with zero manual spacing, hardcoded breaks, or visual overrides.

\subsection{Core Design Principles}
\begin{itemize}
    \item \textbf{Single Responsibility Principle (SRP)}: Presentation macros, state tracking, font loading, geometry boundaries, TikZ overlays, sorting algorithms, lexical sanitization, and serialization are isolated into dedicated modules.
    \item \textbf{State vs. Presentation Decoupling}: \texttt{cg-api.sty} manages dataflow and backend counters; \texttt{cg-macros.sty} handles visual output and page layout.
    \item \textbf{Immutability \& Pure Functions}: Algorithmic operations never mutate global state; they clone inputs and return new structures.
    \item \textbf{Zero TeX Leakage}: TeX control sequences embedded in route parameters are sanitized before entering in-memory state.
\end{itemize}

\subsection{Import Structure \& Namespace Encapsulation}
The engine guarantees zero namespace collision by utilizing orthogonal import strategies for its two primary languages:
\begin{itemize}
    \item \textbf{\LaTeX\ Dependencies}: Handled exclusively via \texttt{\textbackslash RequirePackage} inside the \texttt{.sty} component files. This scopes all visual packages safely.
    \item \textbf{Lua Dependencies}: Handled natively via Lua's \texttt{require("module\_name")}. The \texttt{Makefile} explicitly exposes the \texttt{src/} directory to the Lua compiler by injecting \texttt{LUAINPUTS := \$(abspath src)//:}. This allows the backend to assemble its own logic tree independently of the \TeX\ engine's pathing.
\end{itemize}

\subsection{Module Responsibility Matrix}

\noindent
\begin{tabularx}{\linewidth}{@{} >{\ttfamily\footnotesize}l >{\footnotesize\arraybackslash}X @{}}
\toprule
\normalfont\textbf{Module / Script} & \textbf{Functional Scope} \\
\midrule
climbingguide.lua & In-memory AST state manager and TeX bridge. \\
route\_sorter.lua & Sorting routines enforcing Strict Weak Ordering. \\
sanitize\_tex.lua & Lexical parser stripping macros and checking brace depth. \\
export\_json.lua & RFC-8259 Unicode control character hex-serializer. \\
export\_txt.lua & Fixed-delimiter schema writer with \texttt{N/A} padding. \\
zone\_stats.lua & In-memory tier aggregation and TikZ bar chart rendering. \\
cg-colors.sty & Difficulty color matrix and extended accent palettes. \\
cg-fonts.sty & Binary font loaders and explicit pt-sized semantic aliases. \\
cg-geometry.sty & Physical page dimensions, margins, and centralized lengths. \\
cg-header.sty & Two-sided running header/footer and page style definitions. \\
cg-boxes.sty & TikZ positioning, observation callouts, and summary blocks. \\
cg-api.sty & Internal state tracking, LaTeX3 counters, and I/O triggers. \\
cg-macros.sty & Frontend visual presentation macros and layout boxes. \\
cg-lists.sty & Multi-column alphabetical and difficulty listings. \\
cg-frontmatter.sty & Asymmetric layouts, cover builder, and native ToC styling. \\
\bottomrule
\end{tabularx}
